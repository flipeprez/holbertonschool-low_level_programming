#include <stdio.h>

/**
* * main -first c
* * Return: 0
*/
int main(void)
{
		printf("size of a char is=%i byte\n", sizeof(char));
		printf("size of a int is=%i byte\n", sizeof(int));
		printf("size of a long int is=%i byte\n", sizeof(long int));
		printf("size of a float is=%i byte\n", sizeof(float));
		return (0);
}
